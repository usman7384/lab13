function exponent(a,b){
    return Math.pow(a,b);
}

module.exports.power = exponent;

function round(a){
    return Math.round(a);
}

module.exports.round = round;

function ceiling(a){
    return Math.ceil(a);
}
module.exports.ceiling = ceiling;